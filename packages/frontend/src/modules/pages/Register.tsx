import { observer } from 'mobx-react-lite';
import RegistrationForm from '../auth/components/registration/registration';

const Register = () => {
  return <RegistrationForm />;
};

export default observer(Register);
